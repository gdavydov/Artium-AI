# Postgres / Supabase connection — used by Prisma (schema.prisma, migrations)
PGHOST=aws-0-us-east-1.pooler.supabase.com
PGPORT=6543
PGDATABASE=postgres
PGUSER=postgres.gdavydov
PGPASSWORD=MAr!na@Jan-1961
##DATABASE_URL=postgresql://postgres:MAr!na@Jan-1961@db.<project-ref>.supabase.co:5432/postgres

# Object storage — S3 or Cloudflare R2 (see src/storage/storage.provider.ts)
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
STORAGE_ENDPOINT=
MEDIA_BUCKET=

PORT=4000

# Auth — used to sign/verify JWTs (src/auth). Generate a real secret with:
#   openssl rand -base64 48
JWT_SECRET=

# Used only by prisma/seed-admin.ts (npm run seed:admin) to create the first login
ADMIN_EMAIL=
ADMIN_PASSWORD=
